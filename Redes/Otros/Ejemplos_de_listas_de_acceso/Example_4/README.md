# Ejemplo 4

Una ACL extendida (`Block-ICMP`) bloquea específicamente el tráfico ICMP (`ping`) desde **PC3** (`192.168.5.5`) hacia el servidor (`192.168.4.1`), permitiendo el resto del tráfico para que PC3 pueda seguir accediendo normalmente a los demás servicios, como la web.
