# Ejemplo 2

Se habilita Telnet en las líneas VTY del router con clave `Cisco` y se aplica una ACL estándar por nombre (`ip access-class ping in`) para bloquear el acceso a **PC2** y permitírselo a las demás.
