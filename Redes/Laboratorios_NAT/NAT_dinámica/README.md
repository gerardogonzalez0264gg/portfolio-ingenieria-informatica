# Configuración de NAT dinámica en Packet Tracer

Se configurar **NAT Dinámico con PAT**: consiste en repartir un grupo de IP públicas entre una red privada para salir a Internet, definiendo los puertos, la red permitida, el conjunto de IP públicas y activando la traducción con `overload`.
